{
  "name": "human-sky",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm run build:client && npm run build:server && npm run build:verify",
    "build:client": "cd client && npm install && npm run build && echo '✅ Client build complete' && ls -la dist/",
    "build:server": "cd server && npm install && npm run build && echo '✅ Server build complete'",
    "build:verify": "echo '🔍 Verifying build outputs...' && ls -la client/dist/ && ls -la server/dist/server/",
    "start": "cd server && npm start"
  }
}
