{
  "name": "human-sky",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm install && cd client && npm install && npm run build && cd .. && npm run build:server",
    "build:server": "npm run build --prefix server",
    "build:verify": "echo '🔍 Verifying build outputs...' && echo '📂 Client dist contents:' && ls -R client/dist/ && echo '📂 Server dist contents:' && ls -R server/dist/ && echo '✅ Build verification complete'",
    "start": "cd server && npm start"
  }
}
